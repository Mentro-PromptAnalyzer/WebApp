{
  "name": "mentro-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix mentro",
    "postinstall": "npm ci --prefix mentro",
    "build": "npm run build --prefix mentro",
    "lint": "npm run lint --prefix mentro",
    "test": "npm test --prefix mentro",
    "format": "npm run format --prefix mentro",
    "format:check": "npm run format:check --prefix mentro",
    "test:smoke": "node --test tests/http-smoke.test.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
